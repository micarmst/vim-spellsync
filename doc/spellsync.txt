*spellsync.txt* magically rebuild Vim spell files

               \│/  ╔═╗╔═╗╔═╗╦  ╦  ╔═╗╦ ╦╔╗╔╔═╗  ~
            ──── ─  ╚═╗╠═╝║╣ ║  ║  ╚═╗╚╦╝║║║║    ~
               /│\  ╚═╝╩  ╚═╝╩═╝╩═╝╚═╝ ╩ ╝╚╝╚═╝  ~
                   Magically rebuild spell files ~
                     (plus other incantations)   ~
 
====================================================================
CONTENTS                                           *SpellSyncContents*

    1. Overview.............. |SpellSyncOverview|
    2. Usage ................ |SpellSyncUsage|
    3. Config ............... |SpellSyncConfig|
    4. License .............. |SpellSyncLicense|
    5. Bugs ................. |SpellSyncBugs|
    6. Contributing ......... |SpellSyncContributing|
    7. Changelog ............ |SpellSyncChangelog|

====================================================================
1. Overview                                        *SpellSyncOverview*

SpellSync rebuilds Vim spell files if word lists are modified
externally. One use case would be if word lists are stored in
a source control system like Git and shared across multiple
computers.

Tested with Vim 8.2.1926 or newer (with spell support) and Neovim
0.5.0 or newer.

SpellSync also creates `.gitignore` and `.gitattributes` files in
Vim's spell directories to exclude binary spell files and uses its
union merge driver to avoid conflicts.

Existing Git configuration is left untouched, including unreadable
files and symbolic links. Runtime spell directories without word
lists are also left alone.

====================================================================
2. Usage                                              *SpellSyncUsage*

The plugin runs automatically at startup by default. It can also be
called with the command below. If first loaded after startup, it syncs
immediately. Repeated loading does not register duplicate hooks or sync
again. Set configuration options before loading the plugin; setting
|g:spellsync_run_at_startup| to 0 disables both automatic cases.

                                                        *:SpellSync*
>

    :SpellSync

Read-only word lists can be compiled if the binary spell file can
be created or updated. Missing word lists are skipped.

If a source cannot be read or its binary cannot be written, SpellSync
reports the path and reason, then continues with other dictionaries.
Git configuration write failures and spell-refresh errors are also
reported. Use |:messages| to review warnings after startup or a sync.

If a binary path points to a directory or other non-file entry, the
final spell refresh is deferred until that path is fixed. Other
dictionaries are still rebuilt.

On Vim before 9.1.0783 or Neovim before 0.11, use `set isfname+=92` before assigning a
custom 'spellfile' path containing an escaped comma. SpellSync handles
this limitation itself when refreshing runtime dictionaries, restoring
the original 'isfname' afterwards.

====================================================================
3. Config                                            *SpellSyncConfig*

Below are the options available and their default values.

                                      *g:spellsync_run_at_startup*
Run SpellSync automatically at startup, or immediately when first loaded
later. Set to 0 to use manual syncing only: >

    let g:spellsync_run_at_startup = 1

Enable the Git union merge option. Creates a `.gitattributes` file
in the spell directories if one does not exist: >

    let g:spellsync_enable_git_union_merge = 1

Enable Git ignore for binary spell files. Creates a `.gitignore`
file in the spell directories if one does not exist: >

    let g:spellsync_enable_git_ignore = 1

====================================================================
4. License                                          *SpellSyncLicense*

SpellSync is released under the MIT license. See `LICENSE.MD` for
further details.

====================================================================
5. Bugs                                                *SpellSyncBugs*

Please submit any bug reports to the project's GitHub issue tracker:

    https://github.com/micarmst/vim-spellsync/issues

====================================================================
6. Contributing                                *SpellSyncContributing*

Contributions are welcome, feel free to submit a pull request:

    https://github.com/micarmst/vim-spellsync

====================================================================
7. Changelog                                      *SpellSyncChangelog*

Unreleased
    * FIX: Register startup syncing once and honor startup settings when
           plugin managers load SpellSync after VimEnter
    * FIX: Refresh comma-containing runtime paths on older Vim and make
           warning and file-preservation tests portable to Windows
    * FIX: Compile readable word lists without requiring source write
           access; check existing binary or destination directory access
    * FIX: Report file and refresh failures while continuing the sync
    * FIX: Handle escaped commas and ignore completion filters when
           discovering word lists
    * CHANGE: Leave runtime spell directories without word lists alone
    * FIX: Preserve existing Git configuration entries, including
           unreadable files and dangling symbolic links
    * FIX: Refresh spell checking without changing word lists or
           recompiling an unchanged first custom dictionary
    * FIX: Preserve an empty 'spellfile' option during refresh

v1.0.0
    * ADD: Plugin attribution comment for generated files
        * Suggested by John Karahalis (openjck)
v0.1.0
    * FIX: Learn semantic versioning
    * ADD: Force a spell reload after rebuilding
    * ADD: Configuration options
    * ADD: Write this documentation
    * CHANGE: Refactor code
    * CHANGE: Move main functions to autoload
v0.0.4
    * FIX: Handling multiple custom spell files
        * Reported by Andrew Burns (ErebusBat)
    * FIX: Learn how to write Vimscript
    * CHANGE: Refactor code
v0.0.3
    * ADD: Handle custom spell files
        * Contributed by Gabby Paolucci
v0.0.2
    * Subsequent stable release
v0.0.1
    * Initial unstable release
